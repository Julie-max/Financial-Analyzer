# Trained model files are generated by running: python setup.py
# They are excluded from git via .gitignore
# Run setup.py after cloning to generate:
#   - classifier_pipeline.joblib  (TF-IDF + LinearSVC spend classifier)
#   - crf_parser.joblib           (CRF description parser)
